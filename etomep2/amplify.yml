version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: dist  # Update this if your Vite output directory is different
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

